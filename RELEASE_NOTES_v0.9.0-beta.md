# Local Video Catalog v0.9.0-beta

手元の動画を **このPCの中だけで** 解析して、内容の説明文と
検索できる HTML カタログを作る Windows 向けのツールです。

**これは公開 beta です。** 実運用で 50 本以上の家庭用動画を通して
確認していますが、環境によっては想定外のことが起こりえます。
大切な動画のバックアップを取ったうえでお試しください
（このアプリは元動画を読むだけで、書き換えも削除もしません）。

---

## 主な機能

- 動画フォルダーを丸ごと登録し、台帳ID順に解析する
- 代表静止画の抽出
- ローカル VLM による映像内容の解析と、動画全体の視覚概要
- ローカル Whisper による文字起こし（任意）
- 動画 1 本ごとの説明文（YouTube 概要欄用の短文つき）
- 検索・絞り込みできる HTML カタログ（**解析のたびに自動更新**）
- **安全停止と再開** — 途中で止めても、次回は続きから
- 失敗した動画だけのやり直し
- 中間ファイルの整理（Windows のゴミ箱へ）

---

## プライバシーと安全性

- **映像・画像・音声・文字起こし・説明文を外部へ送信しません**
- 接続先を `127.0.0.1` / `localhost` / `::1` に限定し、HTTP リダイレクトを
  辿らず、プロキシ環境変数を無効化しています。「外部を許可する」設定は
  実装していません
- **元動画は読み取り専用**です。上書き・改名・移動・削除・メタデータの
  書き換えをしません。生成物を元動画のフォルダーへ書きません
- アプリが作るものは、すべて配布フォルダーの `userdata\` の中に置きます。
  `%LOCALAPPDATA%` やレジストリへは何も書きません
- 中間ファイルの整理は `userdata\cache\` の中だけが対象で、完全削除はせず
  Windows のゴミ箱へ送ります。送れなければファイルを残します
- 人物名・人間関係・場所・行事などを、根拠なく断定しません

---

## 動作に必要なもの（同梱していません）

| 必要なもの | 用途 | 必須か |
|---|---|---|
| Windows 10 / 11（64bit） | 動作環境 | 必須 |
| [LM Studio](https://lmstudio.ai/) | 映像の解析・説明文の生成 | **必須** |
| VLM モデル（動作確認済み: `qwen3-vl-8b-instruct`） | 映像の解析 | **必須** |
| [ffmpeg / ffprobe](https://ffmpeg.org/download.html) | 動画情報の取得・静止画の抽出 | **必須** |
| whisper フィルター入りの ffmpeg（8.x 以降のフル版） | 文字起こし | 任意 |
| [Whisper モデル（`ggml-*.bin`）](https://huggingface.co/ggerganov/whisper.cpp) | 文字起こし | 任意 |

**Python のインストールは不要です。** 実行に必要な Python 3.13.14 と
Tcl/Tk を `runtime\` に同梱しています（python.org 公式から取得）。
お使いの PC の Python 環境には触れません。

---

## 既知の制約

- **Windows のみ**です（macOS / Linux では動きません）
- **LM Studio が必須**です。起動していないと解析を開始できません
- **画像を扱えるモデル（VLM）が必須**です。テキスト専用のモデルでは
  開始できません（実際にテスト画像を 1 枚送って確認しています）
- **文字起こしには whisper 対応の ffmpeg と Whisper モデルが必要**です。
  無い場合は「文字起こしを飛ばす」で進められます
- **処理時間は GPU 性能や動画の長さで大きく変わります。**
  一晩で大量に終わる前提の作りにしていません
- クラウド AI には対応していません（対応する予定もありません）
- NAS / ネットワークドライブ向けの最適化はしていません
- 顔認識・人物特定は行いません
- **モデルの自動ダウンロードは行いません**（外部へ取りに行く経路を持ちません）
- インストーラーはありません。ZIP を展開して使います
- 日本語を含むフォルダー名・ファイル名で動作を確認しています

---

## はじめかた

ZIP を書き込みできる場所へ展開し、`Start.cmd` をダブルクリックしてください。

くわしくは同梱の `docs\QUICKSTART.md`、
うまくいかないときは `docs\TROUBLESHOOTING.md` を参照してください。

---

## フィードバックについて

お気づきの点はお知らせください。いただいた内容は今後の改善の参考に
させていただきますが、**すべてのご要望や不具合への対応をお約束するものでは
ありません。**

> Feedback is welcome, but fixes and feature requests are not guaranteed.

---

## ライセンス

本体は MIT License。同梱・外部コンポーネントについては
同梱の `THIRD_PARTY_NOTICES.md` を参照してください。

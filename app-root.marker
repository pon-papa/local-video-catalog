# local-video-catalog APP_ROOT marker
#
# このファイルが置かれているフォルダーが APP_ROOT です。
# アプリが生成・保持するすべての状態は、このフォルダーの
# userdata\ 配下にだけ保存されます。
#
# 移動・削除しないでください。削除すると起動時に APP_ROOT を
# 決められず、アプリは代替場所を勝手に選ばずに停止します。
